{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build and Start Meta Search",
			"type": "shell",
			"command": "cd tunnel-service && npm install && cd admin-ui && npm install && npm run build && cd .. && npm run dev",
			"group": "build",
			"isBackground": true,
			"problemMatcher": [
				"$tsc",
				"$eslint-stylish"
			]
		}
	]
}